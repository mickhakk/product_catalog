import { FC } from "react";

interface Props {
  totalCount: number,
  pageSize: number,
  siblingCount: number,
  currentPage: number,
}
export const Pagination: FC<Props> = ({
  totalCount,
  pageSize,
  siblingCount,
  currentPage,
}) => {
  
return (

)
};
